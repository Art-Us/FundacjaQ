// Fallback center used whenever a gmina has no lat/lng set — this project's
// pilot scope is a single gmina (Nowa Dęba), so this doubles as its default.
export const NOWA_DEBA_CENTER: [number, number] = [50.4166, 21.75];
